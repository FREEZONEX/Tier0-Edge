# MQTT Configuration
# 开发环境使用 localhost，生产环境（构建后）使用 emqx
VITE_MQTT_HOST=localhost
# WebSocket (ws/wss) 使用 8083，TCP (mqtt/mqtts) 使用 1883
# 如果未指定，会根据协议自动选择：ws -> 8083, mqtt -> 1883
VITE_MQTT_PORT=8083
VITE_MQTT_PROTOCOL=ws
VITE_MQTT_PATH=/mqtt

# 生产环境配置示例（构建时设置）:
# VITE_MQTT_HOST=emqx
# VITE_MQTT_PORT=8083  # WebSocket
# 或
# VITE_MQTT_PORT=1883  # TCP

# API Base URL (for Kong gateway or Nginx proxy)
VITE_API_BASE_URL=http://localhost:3000

# UNS Topic Prefix
VITE_ENTERPRISE=Plant_Name
VITE_SITE=SMT-Area-1
VITE_AREA=SMT-Line-1
VITE_LINE=Printer-Cell
VITE_DEVICE=Printer01

